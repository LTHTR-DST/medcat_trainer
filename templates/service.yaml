apiVersion: v1
kind: Service
metadata:
  name: {{ include "medcat_trainer.fullname" . }}
  labels:
    {{- include "medcat_trainer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "medcat_trainer.selectorLabels" . | nindent 4 }}
