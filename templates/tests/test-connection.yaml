apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "medcat_trainer.fullname" . }}-test-connection"
  labels:
    {{- include "medcat_trainer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "medcat_trainer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
